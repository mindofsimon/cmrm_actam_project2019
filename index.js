/* 
Initial Page (Index)
    -just a transition to make logo appear
    -in the HTML page there are two forms to submit login/registration data
    -controls on above data is made on the check login/registration pages (where forms redirect user)
*/

//simple transition to make logo appear
logo_icon.style.opacity = '1';